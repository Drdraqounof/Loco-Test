'use client';

// In plain terms: this file represents the floating button feature, which is mostly handled by Electron.

// This component is now handled by Electron's floating-button.html
// Keeping this file for reference but it's not rendered in Electron mode
export default function FloatingButton() {
  return null;
}
